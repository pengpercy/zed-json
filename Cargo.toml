[package]
name = "json_tools"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = { version = "1.0", features = ["arbitrary_precision"] }
zed_extension_api = "0.7.0"
